{
  "manifest_version": 3,
  "name": "Fact Check (local)",
  "version": "0.1.0",
  "description": "Verify factual claims in selected text or pasted text, using a local fact-checking backend (no cloud, no paid API).",
  "permissions": ["contextMenus", "activeTab", "scripting"],
  "host_permissions": ["http://127.0.0.1:5000/*"],
  "background": {
    "service_worker": "background.js"
  },
  "action": {
    "default_popup": "popup.html",
    "default_title": "Fact Check"
  }
}
